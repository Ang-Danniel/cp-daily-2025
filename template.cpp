#include <bits/stdc++.h>
using namespace std;

signed main() {
    int n; cin >> n;
    int ar[n]; // Deklarasi

    for (int i = 0; i < n; i++) {
        cin >> ar[i]; // Menginput ke indeks i pada array
    } // Masukkan n bilangan ke array!

    string s = "danniel";
    int panjang = s.length(); // Panjang string

    char something = ' ';
    cout << int (something); // Ini konversi char ke ascii integer!
}

// Intinya kalau type nya 1, input 2 bilangan k dan X
// Bila type nya 2, input 1 bilangan k